program ejemploPruebaOperador;
    VAR
      a:INTEGER;
      b:INTEGER;
      c:INTEGER;

BEGIN
    c = a mod b;
    b = a mod c;
    writeln('Prueba de los operadores, div y mod tienen que salir como / t %');
END.
